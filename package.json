{
  "name": "start_template",
  "version": "1.0.0",
  "description": "Boilerplate for start new project",
  "main": "./src/index.html",
  "scripts": {
    "start": "parcel ./src/index.html --open",
    "build": "rm -rf ./dist && parcel build ./src/index.html",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Denys Kondratiev",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^1.12.4"
  }
}
